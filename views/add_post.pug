extends layout

block content
  h1 #{title}
  h1 #{topic.title}
  form(method='post', enctype='multipart/form-data',action='/posts/upload/'+ topic._id)
    #form-group
      label Title:
      input.form-control(name='file',type='file')
    //-#form-group
      label Body:
      textarea.form-control(name='body')  
    br    
    input.btn.btn-primary(type='submit',value='submit')
    
